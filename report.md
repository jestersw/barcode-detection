# Устройство для считывания штрихкодов на конвейере
---

## 1. Задача

Придумать программно-аппаратный комплекс, который решает задачу считывания штрихкодов, наклеенных на картонные коробки

## 2. Исходные данные

| Параметр | Значение |
|----------|----------|
| Ширина конвейера | 650 мм |
| Скорость ленты | 1 м/с |
| Интервал между коробками | 2 с |
| Размер коробки | 600 × 400 × 400 мм |
| Расположение штрихкодов | на любой из 6 граней, возможно несколько на грани |
| Ширина этикетки / штрихкода | 78 мм / 60 мм (полезная зона) |
| Ограничение на размещение устройства | не далее 3 м от секции сортировки |

Пропускная способность: 1 коробка / 2 с = **1800 коробок/час**.

## 3. Допущения по типу штрихкода

На этикетке из ТЗ (`КТЯ - 24652856131000`, 14 цифр + буквенный префикс) присутствует буквенно-цифровая строка, не вмещающаяся в EAN-8/EAN-13. Система при этом рассчитывается на линейные 1D-коды семейств Code 128 и EAN-13 и проектируется по **наихудшему случаю** - минимальному размеру модуля.

Оценка размера модуля X (ширина самого узкого штриха) при полезной ширине кода 60 мм:

| Код | Число модулей | X-модуль |
|-----|---------------|----------|
| EAN-13 | 95 | ≈ 0,63 мм |
| Code 128 (14 цифр) | ≈ 154 | ≈ 0,39 мм |

**Расчетное значение: X = 0,39 мм** (худший случай).

## 4. Требования к сенсору

### 4.1. Выдержка 

Чтобы смаз за время экспозиции не превысил половины модуля при скорости v = 1000 мм/с:

```
t ≤ 0,5X / v = 0,5 * 0,39 / 1000 ≈ 195 мкс
```

**Следствия:**

- Роллинг-шаттер вносит геометрический перекос движущегося объекта.
- За 195 мкс непрерывный свет не дает сенсору набрать достаточный сигнал, поэтому требуется мощная светодиодная вспышка, синхронная с кадром.

### 4.2. Разрешение

Для устойчивого декодирования 1D-кода необходимо ≥ 3 пикселя на модуль (2 px - абсолютный минимум). При поле зрения на грань ≈ 700 мм (грань 600 мм + запас на положение коробки и перспективу):

```
px_поперек = FOV / X(px/модуль) = 700 / 0,39 * 3 ≈ 5385 px
```

| Плотность | px_поперек | Класс камеры |
|-----------|------------|--------------|
| 3 px/модуль (комфорт) | ≈ 5400 | ≈ 20 MP area-scan / line-scan |
| 2 px/модуль (минимум) | ≈ 3600 | 12 MP (≈ 2,3 px/модуль) |

**Вывод: 12 MP - минимальные требования, 20 MP - с запасом.**

### 4.3. Частота кадров

FPS не является узким местом. Коробка находится в зоне тоннеля ≈ 0,6–1 с; при 10 FPS это 6–16 кадров на грань — достаточная избыточность для гарантированного захвата кода. Ограничивающие факторы - выдержка и освещение, а не FPS.

## 5. Выбор сенсоров

### 5.1. Сравнение архитектур

**A. Готовые фиксированные сканеры-ридеры (индастриал стандарт).**
```
Промышленные ридеры (Cognex DataMan 380/580, Datalogic Matrix) в модульном сканирующем тоннеле. Оптика, подсветка и декодер интегрированы, читают несколько кодов за кадр, тоннель достигает >99% чтения при 4–6 ридерах.

Плюсы: максимальная надёжность, работа из коробки.
Минусы: высокая стоимость, blackbox, не демонстрирует ML-компетенцию, требуемую ТЗ.
```

**B. Area-scan камеры + нейросеть детекции + декодер (выбранное решение).**
```
6 промышленных камер по граням, YOLO локализует штрихкоды, ZBar/pyzbar декодирует.

Плюсы: гибкость, соответствие требованиям ТЗ по нейросети и датасету, возможность прототипирования, меньшая стоимость.
Минусы: ответственность за надежность лежит на разработчике.
```

**C. Line-scan камеры.**
```
Построчная съемка по мере движения. 

Плюсы: произвольное разрешение вдоль движения, экономия по пикселям.
Минусы: плохо покрывает боковые и нижнюю грани, геометрия 6 граней ломается.
```

### 5.2. Решение

**Вариант B** (камеры + YOLO): единственный, позволяющий выполнить требования ТЗ по обоснованию архитектуры нейросети и датасета, дешевле и гибче готового тоннеля. **Вариант A** используется в отчете как эталон надежности для сравнения.

### 5.3. Конкретное оборудование 
ПРЕД: анализ от **glm-5.3**

- **Камеры: Hikrobot MV-CH120-11GM × 6.** 12 MP, GigE, монохром, глобальный затвор, сенсор Sony IMX304.
  - Глобальный затвор > требование по смазу.
  - 12 MP > минимальное расчетное разрешение.
  - Монохром > штрихкоду цвет не нужен, выше эффективное разрешение и светочувствительность (критично при 195 мкс).
  - GigE с PoE > простая проводка (питание + данные одним кабелем).
  - Ограничение: 12 MP по 1GigE ≈ 9 FPS (достаточно), для большего - 10GigE/CoaXPress.
- **Объектив: ≈ 20 мм, C-mount.** Расчёт под FOV 700 мм, рабочую дистанцию ≈ 1 м, сенсор 1.1″ (ширина 14,1 мм): `f = 1000 * 14,1 / 700 ≈ 20 мм`. Рабочая дистанция ≤ 3 м укладывается в ограничение ТЗ.
- **Подсветка: импульсные светодиодные линейки + строб-контроллер**, синхронные с триггером. Цвет - красный для высокого контраста на штрихах.
- **Триггер: инкрементальный энкодер на валу конвейера + фотодатчик присутствия** на входе тоннеля. Энкодер задает момент съемки и привязывает кадры к конкретной коробке.

## 6. Физическое расположение сенсоров

Пять воздушных граней покрываются сканирующим тоннелем - П-образной рамой с камерами, установленными под углом, чтобы каждая захватывала несколько граней с перекрытием:

- 1 камера сверху - верхняя грань
- 2 боковые, развернутые вперед под ~45° - левая, правая и частично передняя грани
- 2 боковые, развернутые назад под ~45° - левая, правая и частично задняя грани.

Итого 5 камер покрывают 5 граней, отдельные камеры на переднюю и заднюю грани не требуются.

**Шестая грань (дно)** физически не видна сверху или сбоку. Решение - **gap belt:** между двумя секциями конвейера оставляется щель, под которой 6-я камера (та же модель) смотрит вверх. 

## 7. Вычислитель и схема обработки

### 7.1. Оценка нагрузки
ПРЕД: анализ от **glm-5.3**
На одну коробку (каждые 2 с):

```
6 камер × ≈ 10 кадров × 12 MP (моно) ≈ 60 кадров × 12 МБ ≈ 720 МБ за 1–2 с
```

GPU **не является узким местом**: YOLOv8s на 640 px с TensorRT выдаёт сотни FPS, тогда как требуется ≈ 40 детекций/с. Ограничивающие ресурсы — ввод-вывод (6 GigE-потоков, ≈ 0,7 ГБ/с в пике) и объём памяти для буферизации. Поэтому модуль выбирается по I/O и RAM, а не по TOPS.

### 7.2. Выбор модуля
ПРЕД: анализ от **glm-5.3**

- **Основной: NVIDIA Jetson AGX Orin 32GB.** До 275 TOPS (INT8, sparse; dense-пик ≈ 138 TOPS), встроенный 10GbE, 32 ГБ RAM, зрелый стек JetPack/CUDA/TensorRT. Выбор обусловлен **I/O и памятью** (10GbE и PCIe-линии для агрегации 6 камер, запас RAM под 6-поточную буферизацию), а не пиковыми TOPS.
- **Экономичная альтернатива: Jetson Orin NX 16GB** (до 157 TOPS). Мощности для YOLO достаточно, но требуется carrier-плата для разводки 6 камер, а 16 ГБ RAM работают впритык.
- **Отклонено: Jetson Thor** (≈ 2070 FP4 TFLOPS, ≈ $3499, 130 Вт). Ориентирован на 30B+ LLM, VLA и робототехнику; для задачи чтения штрихкодов избыточен по стоимости и энергопотреблению.

### 7.3. Локальная или облачная обработка

**Обработка - локальная, обязательно:**

- поток ≈ 0,7 ГБ/с физически не передать в облако;
- бюджет задержки: устройство ≤ 3 м от секции сортировки, скорость 1 м/с > ~3 с на все от кадра до решения; сетевой round-trip в облако сюда не укладывается.

**Роль облака — вспомогательная (гибридная схема):** агрегация логов чтения, дообучение модели на пропущенных кодах, OTA-раскатка обновлённой модели на тоннели, дашборды качества. Edge принимает решение локально, облако накапливает данные и раздаёт обновления.

Один AGX Orin обрабатывает 1800 коробок/час с кратным запасом.

## 8. Алгоритм: от кадра до отправки

1. **Триггер и захват.** Фотодатчик фиксирует коробку > энкодер задает позицию > строб-вспышка и синхронный burst-захват со всех 6 камер (несколько кадров на проход для избыточности).
2. **Препроцессинг.** Монохром, вырезание области интереса, подавление шума, выравнивание яркости. Уменьшенная копия - для детектора, полное разрешение - для декода.
3. **Детекция (YOLO).** На уменьшенном кадре сеть находит bounding box'ы штрихкодов.
4. **Кроп и коррекция перспективы.** Область вырезается в полном разрешении, выполняется perspective warp к фронтальному виду (код на грани почти всегда под углом, без коррекции ZBar декодирует нестабильно).
5. **Декод.** ZBar/pyzbar на каждом кропе > строка + проверка контрольной суммы (отсекает ложные чтения).
6. **Агрегация по коробке (трекинг).** Все успешные декоды привязываются к одному box-ID по позиции энкодера и дедуплицируются (один код виден на нескольких кадрах). Результат - множество уникальных кодов коробки.
7. **Отправка.** Список кодов и box-ID передаётся системе сортировки по MQTT/TCP в пределах бюджета ~3 с.
8. **Fallback.** При нулевом чтении или низкой уверенности > флаг `no-read` > коробка в reject-lane / на ручной разбор.

```
Бюджет задержки: захват+строб (~10 мс) > детекция (~15 мс/кадр) >
> декод кропов (~5 мс) > агрегация > отправка. 

Укладывается в 3с с запасом.
```

## 9. Нейросеть и датасет

**Роль сети - только локализация без декодирования.** Сеть отвечает, где код, проверенный алгоритм ZBar отвечает на то, "что в коде". Разделение повышает надежность и упрощает обучение.

**Обоснование применения нейросети.** Картон с текстурой, блики от скотча, перспектива, несколько кодов на грани и посторонние надписи приводят к пропускам и ложным срабатываниям чисто градиентной локализации. YOLO робастнее к фону и ракурсу.


ПРЕД: анализ от **glm-5.3**

**Архитектура: YOLOv8/v11, версия nano или small.** One-stage, real-time на AGX Orin через TensorRT, зрелая экосистема (Ultralytics). Рекомендуется вариант **OBB (oriented bounding box)** — ориентированная рамка точнее для повёрнутых кодов. Отклонённые альтернативы: two-stage (Faster R-CNN) — точнее, но медленнее и избыточен; классический CV — хрупок.

**Датасет:**

- **Классы:** 1 (`barcode`) для 1D; при заделе на 2D — 2 класса (`1D` / `2D`).
- **Объем:** 2000–5000 размеченных изображений для рабочего старта, 10 000+ для продакшна.
- **Разметка:** ориентированные bbox / полигоны вокруг каждого кода (CVAT, Roboflow); балансировка по всем 6 граням, углам, числу кодов на грань.
- **Источник (критично совпадение домена):** съемка на реальном стенде теми же 6 камерами с той же подсветкой. Дополнительно — синтетика: генерация кодов (`python-barcode`) и наложение на фото картона.
- **Аугментации:** motion blur (симуляция смаза при 1 м/с — прямо под задачу), поворот и перспектива, яркость/контраст/блики, гауссов шум, помятость и частичное перекрытие, изменение масштаба.
- **Обучение:** transfer learning с COCO-претрейна, fine-tuning.

## 10. Обоснование библиотек и фреймворков

- **OpenCV** — препроцессинг, коррекция перспективы, работа с кропами.
- **Ultralytics YOLO (PyTorch)** — детекция; **TensorRT** — ускоренный инференс на Jetson.
- **NVIDIA DeepStream / GStreamer** — аппаратно ускоренный пайплайн мульти-камерного захвата на Jetson (разгрузка CPU на VIC/NVDEC).
- **pyzbar / ZBar** — декодирование 1D/2D + контрольная сумма.
- **paho-mqtt** — интеграция с системой сортировки.


## 11. Метрики качества

**ML-метрики (детекция):** mAP@0.5, precision / recall по классу `barcode`, отдельно recall на мелких и смазанных кодах.

**Системные метрики (приоритетные):**

- **Read rate** — доля коробок, где считаны все коды. Целевой ориентир: > 99%.
- **No-read rate** — доля коробок без единого чтения.
- **Misread rate** — доля неверно декодированных кодов. Критична (ведёт к неверной сортировке), удерживается около нуля контрольной суммой ZBar.
- **Throughput** — коробок/час против требуемых 1800.
- **Latency** — время кадр → отправка в пределах бюджета.

Методика измерения: прогон на реальном конвейере с ground-truth списком кодов для каждой коробки.

## 12. Масштабирование

- **Другие форм-факторы коробок:** регулируемая высота арки тоннеля, объективы с запасом глубины резкости, пересчет разрешения. Модульная конструкция тоннеля допускает перемещение и добавление камер.
- **Другие типы штрихкодов:** ZBar поддерживает EAN, Code 128, QR, DataMatrix и др. - переключение символогий происходит в декодере, без замены железа. Переход с 1D к 2D = дообучение детектора на 2D-класс + флаг декодера.
- **Скорость ленты выше 1 м/с:** короче выдержка > мощнее строб, при необходимости выше FPS (10GigE/CoaXPress). Ограничение упрется в мощность подсветки.
- **Больше линий:** одна модель обслуживает N тоннелей, облако раздает OTA-обновления.

## 13. Демонстрационный прототип 

https://github.com/jestersw/barcode-detection



